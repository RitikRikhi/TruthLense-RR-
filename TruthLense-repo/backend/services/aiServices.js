import axios from 'axios';
//import { GoogleGenerativeAI } from '@google/generative-ai';

//const extractClaim = async (text) => {
//    console.log("the function is called")
//  try {
//
//    const extractClaim=async(text)=>{
//      console.log("Started extraction");
//      const sentence=text.split(/)
//
 // if (!process.env.GEMINI_API_KEY) {
 //   throw new Error("GEMINI_API_KEY is not defined in environment variables");
 // }
 // 
 // console.log("API KEY:", process.env.GEMINI_API_KEY);

  //  const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
  //  const model = genAI.getGenerativeModel({ model: "gemini-2.0-flash" });
//
  //    const prompt = ` Extract the main factual claim from the following text.
  //  Return only one short sentence.
//
  //  Text:
  //  ${text}
  //  `;

//   const result=await model.generateContent(prompt);
//   const response = await result.response;
// const claim=response.text();
//  console.log("Extracted Claim:", claim);
//  return claim;
//
// } catch (error) {
//   console.error("Error extracting claim:", error.response?.data || error.message);
//   return "Error extracting claim";
// }
//;
//


//export default extractClaim;

